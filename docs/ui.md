# UI

Launch:

```bash
ollama-remote ui
```

The UI:

- is optional (CLI remains primary)
- binds to `127.0.0.1` only
- executes the official `ollama` CLI under the hood
- does not persist prompt history by default
